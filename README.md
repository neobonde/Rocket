# Rocket
Python implementation of a game i once worked on i c++, but was lost.


## Used Packages
Python packages used by this project.   
Install them using pip, syntax example ```python -m pip install <package>```
 * PyOpenGL 
 * PyOpenGL_accelerate
 * Pygame